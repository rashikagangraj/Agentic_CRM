version: '3.8'

services:
  web:
    image: rashikag/agentic-crm:1.0.6
    restart: always
    ports:
      - "80:8100"
    environment:
      - NODE_ENV=production
      - PORT=8100
      - HOSTNAME=0.0.0.0
      - GEMINI_API_KEY=${GEMINI_API_KEY}
